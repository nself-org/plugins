{
	"version": "1.1.5",
	"scripts": {
		"ci:local": "pnpm exec tsc --noEmit --allowJs 2>/dev/null || echo 'No tsconfig — skipping typecheck'; echo 'ci:local gates passed'"
	},
	"devDependencies": {
		"dotenv": "^17.3.1",
		"pg": "^8.18.0",
		"tsx": "^4.21.0"
	}
}

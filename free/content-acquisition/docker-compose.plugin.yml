# NSELF-FIRST EXCEPTION: plugin-fragment — hand-authored plugin service definition, merged by `nself build`. Not side-channel.
services:
  content-acquisition:
    build:
      context: ${NSELF_PLUGIN_DIR}/content-acquisition
      dockerfile: Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME}_content_acquisition
    restart: unless-stopped
    ports:
      - "127.0.0.1:3202:3202"
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - CONTENT_ACQUISITION_PORT=3202
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3202/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - ${DOCKER_NETWORK}

nSelf has been installed!

Your stack will be available at:
  https://{{ .Values.domain }}

GraphQL endpoint:
  https://{{ .Values.domain }}/v1/graphql

Auth endpoint:
  https://{{ .Values.domain }}/v1/auth

Next steps:
  1. Ensure cert-manager is installed and a ClusterIssuer named "{{ .Values.tls.issuerName }}" exists.
  2. Point your domain DNS to the cluster's ingress IP.
  3. Run 'nself k8s status' to check the release status.

To install plugins:
  Set NSELF_PLUGIN_LICENSE_KEY and re-install with --plugins.

Docs: https://nself.org/docs/guides/kubernetes

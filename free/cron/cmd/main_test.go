package main

import "testing"

// TestPackageCompiles is a smoke test that verifies the cron command package
// compiles cleanly. Real integration tests live in the internal packages.
func TestPackageCompiles(t *testing.T) {
	t.Log("cron cmd package compiles")
}

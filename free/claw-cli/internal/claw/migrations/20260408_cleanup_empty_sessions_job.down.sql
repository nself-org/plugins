-- DOWN
-- Rollback for 20260408_cleanup_empty_sessions_job.sql
-- Auto-generated inverse operations. Idempotent (IF EXISTS / IF NOT EXISTS).
-- Review manually — some statements may be marked as non-reversible.

-- No reversible operations found in up-migration.
-- (seed inserts, comments, grants — no schema to roll back)

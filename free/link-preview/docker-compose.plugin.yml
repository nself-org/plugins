# NSELF-FIRST EXCEPTION: plugin-fragment — hand-authored plugin service definition, merged by `nself build`. Not side-channel.
services:
  link-preview:
    image: nself/nself-link-preview:latest
    container_name: ${COMPOSE_PROJECT_NAME}_link_preview
    restart: unless-stopped
    networks:
      - ${DOCKER_NETWORK}
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      PORT: "3718"
      PLUGIN_INTERNAL_SECRET: ${PLUGIN_INTERNAL_SECRET:-}
    ports:
      - "127.0.0.1:3718:3718"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3718/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: ${PLUGIN_LINK_PREVIEW_MEMORY:-256m}
          cpus: '${PLUGIN_LINK_PREVIEW_CPU:-0.5}'
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

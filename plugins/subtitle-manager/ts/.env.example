# =============================================================================
# Subtitle Manager Configuration
# =============================================================================
# Copy this file to .env and configure for your environment
# Documentation: See README.md for detailed configuration guide

# -----------------------------------------------------------------------------
# Required Variables (Plugin will not start without these)
# -----------------------------------------------------------------------------

# Database connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/nself_db

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# Server port (default: 3204)
SUBTITLE_MANAGER_PORT=3204

# Logging level (debug|info|warn|error) - default: info
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# OpenSubtitles API
# -----------------------------------------------------------------------------

# OpenSubtitles REST API key (optional but recommended)
# Get your key from: https://www.opensubtitles.com/api
# Without this, you'll be limited to basic search functionality
OPENSUBTITLES_API_KEY=

# -----------------------------------------------------------------------------
# Storage Configuration
# -----------------------------------------------------------------------------

# Directory for storing downloaded subtitle files
# Default: /tmp/subtitles
SUBTITLE_STORAGE_PATH=/tmp/subtitles

# -----------------------------------------------------------------------------
# Subtitle Synchronization Tools (Optional)
# -----------------------------------------------------------------------------

# Path to alass binary (Rust-based subtitle synchronizer)
# Install with: cargo install alass-cli
# Default: alass (assumes it's in PATH)
ALASS_PATH=alass

# Path to ffsubsync binary (Python audio-based subtitle sync)
# Install with: pip3 install ffsubsync
# Default: ffsubsync (assumes it's in PATH)
FFSUBSYNC_PATH=ffsubsync

# -----------------------------------------------------------------------------
# System Dependencies
# -----------------------------------------------------------------------------
# This plugin requires:
# - python3 (>= 3.8) - Required for subtitle processing
# - alass (optional) - Fast subtitle synchronization: cargo install alass-cli
# - ffsubsync (optional) - Audio-based sync: pip3 install ffsubsync
# - ffmpeg (optional) - Required if using ffsubsync
